apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: edgar-data
  namespace: gmr
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: edgar-data-pv
  resources:
    requests:
      storage: 50Gi
